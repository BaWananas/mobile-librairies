import {NgModule} from '@angular/core';
import {CommonModule} from '@angular/common';

const DECLARATION = [
];

@NgModule({
  declarations: [
    DECLARATION,
  ],
  imports: [
    CommonModule,
  ],
  exports: [
    DECLARATION,
  ],
})
export class SharedModule { }
